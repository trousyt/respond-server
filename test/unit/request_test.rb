require 'test_helper'

class RequestTest < ActiveSupport::TestCase
  test "the truth" do
    assert false, "Something bad happened"
  end
  
#Ideally, you would like to include a test for everything 
#which could possibly break. It’s a good practice to have at 
#least one test for each of your validations and at least one 
#test for every method in your model.
end
